help command accessed: 
	This command provides functionalities commands and what each functionality does.

-> "-n get -s local" 
	Gets all network interfaces from local device where this program is running on.

->  "n- listen" 
	Listens to all packets from a selected network interface for a specific:
		Snapshot lenght
		Read timeout
		Maximum amount of pactures users wants to capture.

-> "-h ping"
	Pings Host to get IP address of host and router; However main functionality is for user to use
	arp command and get MAC address of host device.

-> "-p filter"
	Filters packet information based on PCAP file that can be read from.


